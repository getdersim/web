#settings.modal.bottom-sheet
  .modal-content
    if user
      h5 Merhaba 
        b.name #{user.name}
    else
      h5 Merhaba 
        b.name
    p Ders.im'de ders materyali paylaşarak, sorulan soruları yanıtlayarak kredi kazanabilir, bu kredileri indirim kuponu ve ödülleri alırken kullanabilirsiniz.
    .collection
      if user
        a.collection-item.blue-text.text-darken-2.center-align(href='/@'+user.slug) Profil
          | 
          i.fa.fa-id-badge(aria-hidden='true')
      if home      
        a.collection-item.blue-text.center-align.modal-trigger.modalLink#uploadModalTrigger(href="#uploadModal") Ders notu yükle
          | 
          i.fa.fa-id-cloud-upload-alt(aria-hidden='true')
      a.collection-item.red-text.text-lighten-1.center-align.modalLink.doLogout Çıkış yap 
        |
        i.fa.fa-sign-out.red-text.text-lighten-1(aria-hidden='true')
